# Opinly blog SDK — server-side API key.
# Get it from Opinly → Settings → Developers. Set locally in .env.local and in
# Vercel → Project → Settings → Environment Variables (Production + Preview).
OPINLY_API_KEY=

# Zapier Catch Hook that receives quote-form submissions (already set in Vercel).
ZAPIER_QUOTE_WEBHOOK_URL=
